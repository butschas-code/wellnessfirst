/**
 * Public pages: hero-flush top main. Body ambient scroll (`wfg-home-body`) is applied in `BaseLayout`
 * for all marketing routes.
 */
export const marketingPageLayout = {
  mainClass: '!pt-0 wfg-home-main',
} as const;
